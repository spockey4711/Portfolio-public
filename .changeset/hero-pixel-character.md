---
"portfolio": minor
---

Add a full-body pixel-art character (me) beside the hero copy. On large screens it shares
the row with the type, foot-aligned to the copy's baseline; on phones it drops below the
CTAs, centred and height-capped. The freestanding transparent PNG lives at
`public/images/hero-avatar.png`, is rendered small (crisp on retina, `priority`-loaded above
the fold) and reveals last in the hero entrance sequence. Adds a `hero.visual.portraitAlt`
copy string (de/en) for its alt text.
