// Synthesized bundle entry for design-sync (this repo is a Next.js app, not a
// published package, so there is no dist entry). Re-exports the reusable UI
// primitives from components/ui so the converter can build them into
// window.PortfolioDS. Paths resolve via the repo tsconfig `@/*` alias.
export { Button } from "@/components/ui/Button";
export { Card } from "@/components/ui/Card";
export { Divider } from "@/components/ui/Divider";
export { MonoLabel } from "@/components/ui/MonoLabel";
export { Pill } from "@/components/ui/Pill";
export { SectionHeader } from "@/components/ui/SectionHeader";
